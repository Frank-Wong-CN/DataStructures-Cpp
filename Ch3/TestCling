.L Load
for (int i = 0; i < 100; i++) {
	int N = RdGen::Random(100000, 10000000);
	auto arr = RdGen::ArrGen<IntType>(N, 0, 1000000);
	RadixSort_Test(arr, N);
	//for (int i = 0; i < N; i++) std::cout << arr[i] << (i == N - 1 ? '\n' : ' ');
}